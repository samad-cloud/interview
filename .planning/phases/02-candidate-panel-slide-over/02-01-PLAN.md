---
phase: 02-candidate-panel-slide-over
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/dashboard/CandidatePanel.tsx
autonomous: true
requirements: [PANEL-01]

must_haves:
  truths:
    - "Clicking a candidate row opens a 640px slide-over from the right edge at full viewport height"
    - "Clicking the backdrop overlay closes the panel"
    - "Pressing Escape closes the panel"
    - "Table scroll position is preserved when panel opens and closes"
    - "Panel uses CSS translateX transition (not Dialog centering)"
  artifacts:
    - path: "frontend/components/dashboard/CandidatePanel.tsx"
      provides: "Slide-over shell — overlay + panel, open/close mechanics, keyboard dismiss, Escape handler"
      exports: ["CandidatePanel"]
    - path: "frontend/app/dashboard/page.tsx"
      provides: "CandidatePanel wired to setSelectedCandidate, existing Dialog left in place until 02-04"
      contains: "CandidatePanel"
  key_links:
    - from: "frontend/app/dashboard/page.tsx"
      to: "frontend/components/dashboard/CandidatePanel.tsx"
      via: "import and render alongside existing Dialog"
      pattern: "CandidatePanel"
    - from: "CandidatePanel"
      to: "document keydown"
      via: "useEffect addEventListener with cleanup"
      pattern: "Escape"
---

<objective>
Build the CandidatePanel slide-over shell: a fixed-positioned 640px right-side panel with a semi-transparent overlay, CSS translateX open/close animation, Escape key dismiss, and click-outside-to-close. This is the structural foundation all subsequent plans build inside.

Purpose: Replace the existing cramped Dialog modal with a full-height panel that has room for all candidate data sections.
Output: `CandidatePanel.tsx` with shell structure (overlay + panel + placeholder content areas), wired into page.tsx alongside the existing Dialog (Dialog removed in plan 02-04).
</objective>

<execution_context>
@C:/Users/Admin3k/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin3k/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-candidate-panel-slide-over/02-RESEARCH.md

@frontend/app/dashboard/page.tsx
@frontend/components/dashboard/CandidateTableRow.tsx
</context>

<interfaces>
<!-- Key types extracted from page.tsx for the executor -->

From frontend/app/dashboard/page.tsx:

```typescript
interface FullVerdict {
  technicalScore: number;
  verdict: string;
  summary: string;
  technicalStrengths: string[];
  technicalGaps: string[];
  recommendation: string;
}

interface FullDossier {
  probeQuestions: { question: string; targetClaim: string; probeType: string }[];
  candidateStrengths: string[];
  areasToProbe: string[];
  overallAssessment: string;
}

interface Round3FullVerdict {
  round3Score: number;
  ultimateVerdict: string;
  executiveSummary: string;
  keyStrengths: string[];
  keyGaps: string[];
  redFlagsResolved: string[];
  remainingConcerns: string[];
  finalRecommendation: string;
}

interface Candidate {
  id: number;
  full_name: string;
  email: string;
  rating: number | null;
  round_2_rating: number | null;
  combined_score: number | null;
  jd_match_score: number | null;
  ai_summary: string | null;
  interview_notes: InterviewNotes | null;
  status: string;
  current_stage: string | null;
  interview_transcript: string | null;
  round_2_transcript: string | null;
  resume_text: string | null;
  job_id: string | null;
  job_title?: string;
  final_verdict: string | null;
  interview_token: string | null;
  created_at: string | null;
  video_url: string | null;
  round_2_video_url: string | null;
  round_3_status: string | null;
  round_3_transcript: string | null;
  round_3_recording_url: string | null;
  round_3_rating: number | null;
  round_3_full_verdict: Round3FullVerdict | null;
  full_verdict: FullVerdict | null;
  round_1_full_dossier: FullDossier | null;
  hr_notes: string | null;
  resume_url: string | null;
  applied_at: string | null;
  round_1_completed_at: string | null;
  round_2_completed_at: string | null;
}

// Existing state that CandidatePanel will hook into:
const [selectedCandidate, setSelectedCandidate] = useState<Candidate | null>(null);

// Existing handlers (signatures):
const handleInviteRound2 = async (candidateId: number) => { ... }
const handleInviteRound3 = async (candidateId: number) => { ... }
const handleRejectClick = useCallback(async (candidate: Candidate) => { ... }, [...])
const handleSaveNote = async (candidateId: number) => { ... }
```

From page.tsx line 1565 — existing Dialog to keep intact until plan 02-04:
```typescript
<Dialog open={!!selectedCandidate} onOpenChange={(open) => { if (!open) { setSelectedCandidate(null); setInterviewNotes(null); } }}>
```

Row click wiring (line 1475):
```typescript
onView={(c) => setSelectedCandidate(c as Candidate)}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create CandidatePanel shell component</name>
  <files>frontend/components/dashboard/CandidatePanel.tsx</files>
  <action>
Create `frontend/components/dashboard/CandidatePanel.tsx` as a 'use client' component.

**Props interface** (copy these types inline into the file — do NOT import from page.tsx):

```typescript
// Inline re-declarations of the types needed (page.tsx does not export them)
interface FullVerdict { technicalScore: number; verdict: string; summary: string; technicalStrengths: string[]; technicalGaps: string[]; recommendation: string; }
interface FullDossier { probeQuestions: { question: string; targetClaim: string; probeType: string }[]; candidateStrengths: string[]; areasToProbe: string[]; overallAssessment: string; }
interface Round3FullVerdict { round3Score: number; ultimateVerdict: string; executiveSummary: string; keyStrengths: string[]; keyGaps: string[]; redFlagsResolved: string[]; remainingConcerns: string[]; finalRecommendation: string; }

interface PanelCandidate {
  id: number;
  full_name: string;
  email: string;
  rating: number | null;
  round_2_rating: number | null;
  status: string;
  job_title?: string;
  created_at: string | null;
  applied_at: string | null;
  video_url: string | null;
  round_2_video_url: string | null;
  round_3_recording_url: string | null;
  round_3_rating: number | null;
  round_3_transcript: string | null;
  round_3_full_verdict: Round3FullVerdict | null;
  full_verdict: FullVerdict | null;
  round_1_full_dossier: FullDossier | null;
  interview_transcript: string | null;
  round_2_transcript: string | null;
  hr_notes: string | null;
  final_verdict: string | null;
}

interface CandidatePanelProps {
  candidate: PanelCandidate | null;
  open: boolean;
  onClose: () => void;
  onInviteR2: (id: number) => void;
  onInviteR3: (id: number) => void;
  onReject: (candidate: PanelCandidate) => void;
  onSaveNote: (id: number, noteText: string) => void;
}
```

**Shell structure** — use the exact CSS from the research doc (Pattern 1):

```typescript
'use client';

import { useEffect, useState } from 'react';
import { X } from 'lucide-react';

export function CandidatePanel({ candidate, open, onClose, onInviteR2, onInviteR3, onReject, onSaveNote }: CandidatePanelProps) {
  // Escape key dismiss (Pattern 8 from research)
  useEffect(() => {
    if (!open) return;
    const handler = (e: KeyboardEvent) => { if (e.key === 'Escape') onClose(); };
    document.addEventListener('keydown', handler);
    return () => document.removeEventListener('keydown', handler);
  }, [open, onClose]);

  if (!candidate) return null;

  return (
    <>
      {/* Overlay — click-outside-to-close */}
      <div
        className={`fixed inset-0 z-50 bg-black/60 transition-opacity duration-300 ${
          open ? 'opacity-100' : 'opacity-0 pointer-events-none'
        }`}
        onClick={onClose}
        aria-hidden="true"
      />

      {/* Panel — slides from right */}
      <div
        className={`fixed right-0 top-0 bottom-0 z-50 w-[640px] bg-[#0F172A] border-l border-[#1E293B] flex flex-col shadow-[-20px_0_60px_rgba(0,0,0,0.5)] transition-transform duration-300 ease-[cubic-bezier(0.16,1,0.3,1)] ${
          open ? 'translate-x-0' : 'translate-x-full'
        }`}
        role="dialog"
        aria-label={candidate.full_name}
        aria-modal="true"
      >
        {/* Header — flex-shrink-0, populated in plan 02-02 */}
        <div className="px-5 py-4 border-b border-[#1E293B] flex items-center justify-between flex-shrink-0">
          <div className="flex items-center gap-3">
            <div className="w-10 h-10 rounded-full bg-[#6366F1] flex items-center justify-center text-white text-sm font-bold flex-shrink-0">
              {candidate.full_name.charAt(0).toUpperCase()}
            </div>
            <div>
              <p className="text-[14px] font-semibold text-[#F9FAFB]">{candidate.full_name}</p>
              <p className="text-[12px] text-[#94A3B8]">{candidate.job_title || 'No role specified'}</p>
            </div>
          </div>
          <button
            onClick={onClose}
            className="w-7 h-7 flex items-center justify-center rounded-md text-[#6B7280] hover:text-[#F9FAFB] hover:bg-[#1E293B] transition-colors"
            aria-label="Close panel"
          >
            <X className="w-4 h-4" />
          </button>
        </div>

        {/* Body — flex-1 overflow-y-auto, sections added in 02-02 and 02-03 */}
        <div className="flex-1 overflow-y-auto px-5 py-4 flex flex-col gap-4">
          <p className="text-[12px] text-[#6B7280]">Content sections — implemented in plans 02-02 and 02-03</p>
        </div>

        {/* Footer — flex-shrink-0, implemented in plan 02-04 */}
        <div className="px-5 py-3 border-t border-[#1E293B] flex gap-2 flex-shrink-0">
          <p className="text-[12px] text-[#6B7280]">Actions — implemented in plan 02-04</p>
        </div>
      </div>
    </>
  );
}
```

**Critical notes:**
- Do NOT add `overflow: hidden` to body — it would reset table scroll position (Pitfall from research doc)
- Panel uses `position: fixed` — does NOT interact with table scroll container, scroll position preserved automatically
- `if (!candidate) return null` prevents rendering when no candidate is selected
- Avoid dynamic Tailwind class names with hex values — use `style={{}}` for any per-candidate colors (established Phase 1 pattern)
  </action>
  <verify>
    <automated>cd C:/Users/Admin3k/Documents/interview/frontend && yarn build 2>&1 | tail -20</automated>
  </verify>
  <done>CandidatePanel.tsx exists, exports CandidatePanel, TypeScript build passes with no errors</done>
</task>

<task type="auto">
  <name>Task 2: Wire CandidatePanel into page.tsx (alongside existing Dialog)</name>
  <files>frontend/app/dashboard/page.tsx</files>
  <action>
Add CandidatePanel to page.tsx alongside the existing Dialog. The Dialog is NOT removed yet — that happens in plan 02-04 after all content sections are built and verified.

**Import to add** (near the other dashboard component imports at line ~104):
```typescript
import { CandidatePanel } from '@/components/dashboard/CandidatePanel';
```

**Render CandidatePanel** — add it immediately before or after the existing `<Dialog open={!!selectedCandidate}...>` block (around line 1565). Place it AFTER the Dialog closing tag so both are present during development:

```typescript
<CandidatePanel
  candidate={selectedCandidate}
  open={!!selectedCandidate}
  onClose={() => { setSelectedCandidate(null); setInterviewNotes(null); }}
  onInviteR2={(id) => handleInviteRound2(id)}
  onInviteR3={(id) => handleInviteRound3(id)}
  onReject={(c) => handleRejectClick(c as Parameters<typeof handleRejectClick>[0])}
  onSaveNote={(id, _noteText) => handleSaveNote(id)}
/>
```

**TypeScript note:** The `PanelCandidate` type in CandidatePanel.tsx is a structural subset of `Candidate` in page.tsx. TypeScript will accept the `selectedCandidate` (typed as `Candidate | null`) as valid because it satisfies PanelCandidate's shape — no cast needed on the prop itself. For `onReject`, use `c as Parameters<typeof handleRejectClick>[0]` since handleRejectClick expects the full `Candidate` type.

The page now renders both the old Dialog AND the new panel simultaneously — both will open on row click. This is intentional for this plan so we can compare. Plan 02-04 removes the Dialog.
  </action>
  <verify>
    <automated>cd C:/Users/Admin3k/Documents/interview/frontend && yarn build 2>&1 | tail -20</automated>
  </verify>
  <done>page.tsx imports and renders CandidatePanel, build passes, clicking a row opens both panel (right side) and old dialog (centered) — confirming wiring is correct</done>
</task>

</tasks>

<verification>
- `CandidatePanel.tsx` exists at `frontend/components/dashboard/CandidatePanel.tsx`
- `page.tsx` imports and renders `<CandidatePanel>`
- `cd frontend && yarn build` exits with code 0, no TypeScript errors
- In browser: clicking a candidate row opens the 640px slide-over from the right (animation visible)
- In browser: clicking outside the panel (on overlay) closes it
- In browser: pressing Escape closes the panel
- In browser: table scroll position does NOT reset when panel opens/closes
</verification>

<success_criteria>
- CandidatePanel shell is a 640px fixed right-side panel with translateX animation
- Overlay dims background and closes panel on click
- Escape key closes panel with cleanup (no event listener leak)
- Build is TypeScript-clean
- Table scroll position preserved (no body overflow manipulation)
</success_criteria>

<output>
After completion, create `.planning/phases/02-candidate-panel-slide-over/02-01-SUMMARY.md`
</output>
