---
phase: 04-settings-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/settings/page.tsx
  - frontend/components/AppSidebar.tsx
autonomous: true
requirements: [SETT-01, SETT-09]

must_haves:
  truths:
    - "/settings is reachable from the sidebar Settings nav link"
    - "Unauthenticated users are redirected to /login?redirect=/settings"
    - "Five tabs render: Company, Email & Comms, Interviews, Scoring, Job Boards"
    - "Switching tabs swaps content without a page reload"
    - "Placeholder content renders in each tab (no blank white boxes)"
  artifacts:
    - path: "frontend/app/settings/page.tsx"
      provides: "Settings page shell with auth guard and five-tab layout"
    - path: "frontend/components/AppSidebar.tsx"
      provides: "Settings nav item added to sidebar"
  key_links:
    - from: "frontend/components/AppSidebar.tsx"
      to: "/settings"
      via: "navItems href entry"
    - from: "frontend/app/settings/page.tsx"
      to: "supabase-browser"
      via: "useEffect getSession redirect"
---

<objective>
Create the /settings route, auth-guard shell, and five-tab navigation component. This is the structural foundation all other Phase 4 plans build on top of.

Purpose: Establishes the page, sidebar link, auth protection, and tab switching — every subsequent plan fills one tab with real content.
Output: frontend/app/settings/page.tsx (auth guard + Tabs shell with placeholder tab bodies), AppSidebar.tsx updated with Settings link.
</objective>

<execution_context>
@C:/Users/Admin3k/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin3k/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-settings-panel/04-RESEARCH.md

<interfaces>
<!-- From frontend/components/AppSidebar.tsx — navItems pattern to follow -->
<!-- Add to navItems array: { label: 'Settings', href: '/settings', icon: <Settings className="w-4 h-4 shrink-0" /> } -->
<!-- Import: import { Settings } from 'lucide-react' -->

<!-- Auth guard pattern (same as dashboard): -->
```typescript
// 'use client'
import { createClient } from '@/lib/supabase-browser';
// useEffect(() => {
//   supabase.auth.getSession().then(({ data }) => {
//     if (!data.session) router.push('/login?redirect=/settings');
//   });
// }, []);
```

<!-- shadcn/ui Tabs API: -->
```typescript
import { Tabs, TabsList, TabsTrigger, TabsContent } from '@/components/ui/tabs';
// <Tabs defaultValue="company">
//   <TabsList>
//     <TabsTrigger value="company">Company</TabsTrigger>
//   </TabsList>
//   <TabsContent value="company">...</TabsContent>
// </Tabs>
```

<!-- Design tokens: bg #020617, surface #0F172A, border #1E293B, primary #6366F1 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Settings link to AppSidebar and create settings page shell</name>
  <files>frontend/components/AppSidebar.tsx, frontend/app/settings/page.tsx</files>
  <action>
**AppSidebar.tsx:** Read the file first. Find the navItems array. Add a Settings entry with href="/settings" and the Settings icon from lucide-react. Add `import { Settings } from 'lucide-react'` if not already present. Position it after the existing nav items (before any footer items if any exist).

**frontend/app/settings/page.tsx:** Create this new file. Mark `'use client'`. Implement:

1. Auth guard: import `createClient` from `@/lib/supabase-browser`, import `useRouter` from `next/navigation`. In a `useEffect`, call `supabase.auth.getSession()` and redirect to `/login?redirect=/settings` if no session.

2. Five-tab layout using shadcn/ui `Tabs` (defaultValue="company"):
   - TabsTrigger values: "company", "email", "interviews", "scoring", "job-boards"
   - Labels: Company, Email & Comms, Interviews, Scoring, Job Boards
   - Add lucide icons next to each label: Building2 (Company), Mail (Email & Comms), Video (Interviews), BarChart2 (Scoring), Briefcase (Job Boards)

3. Each TabsContent renders a placeholder `<div>` with the tab name so content is visible: e.g. `<div className="py-8 text-muted-foreground text-sm">Company settings coming soon</div>`. These will be replaced in plans 04-02 through 04-05.

4. Page layout: `<div className="flex flex-col flex-1 p-6 gap-6 min-h-0">` wrapping a page header (h1 "Settings" + p subtitle "Manage your workspace configuration") and the Tabs block.

5. TabsList styling: `className="flex gap-1 border-b border-border bg-transparent rounded-none w-full justify-start h-auto p-0 mb-6"`. Each TabsTrigger: `className="flex items-center gap-2 px-4 py-2.5 text-sm font-medium text-muted-foreground rounded-none border-b-2 border-transparent data-[state=active]:border-indigo-500 data-[state=active]:text-foreground bg-transparent hover:text-foreground transition-colors"`.

DO NOT import tab content components yet — they don't exist. Placeholder divs only.
DO NOT call any Supabase write methods — read-only auth check only (SETT-09).
  </action>
  <verify>
    <automated>cd /c/Users/Admin3k/Documents/interview/frontend && yarn build 2>&1 | tail -20</automated>
  </verify>
  <done>yarn build passes with zero TypeScript errors. /settings page file exists. AppSidebar.tsx contains Settings href entry.</done>
</task>

</tasks>

<verification>
- `frontend/app/settings/page.tsx` exists with auth guard and 5-tab Tabs component
- `frontend/components/AppSidebar.tsx` contains `/settings` navItem with Settings icon
- `yarn build` passes (zero TS errors)
- No Supabase write calls anywhere in the new file
</verification>

<success_criteria>
Navigating to /settings in dev server shows a page with 5 clickable tabs and placeholder content in each. Sidebar shows a Settings link that navigates to /settings. Unauthenticated visit redirects to /login.
</success_criteria>

<output>
After completion, create `.planning/phases/04-settings-panel/04-01-SUMMARY.md`
</output>
