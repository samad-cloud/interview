---
phase: 04-settings-panel
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - frontend/components/settings/CompanyTab.tsx
  - frontend/app/settings/page.tsx
autonomous: true
requirements: [SETT-02, SETT-09]

must_haves:
  truths:
    - "Company tab renders logo upload area, brand colour picker, and all 6 text/select fields"
    - "Selecting a file shows an image preview via URL.createObjectURL()"
    - "Brand colour picker updates a visible colour swatch in real time"
    - "All fields are controlled inputs backed by local useState — no Supabase writes"
  artifacts:
    - path: "frontend/components/settings/CompanyTab.tsx"
      provides: "Company tab with logo upload, colour picker, name/about/industry/website/HQ/size fields"
  key_links:
    - from: "frontend/app/settings/page.tsx"
      to: "frontend/components/settings/CompanyTab.tsx"
      via: "import + TabsContent replacement"
---

<objective>
Implement the Company tab: logo upload UI with image preview, native colour picker with swatch, and six profile fields (company name, about, industry, website, HQ, company size). All state is local — no persistence.

Purpose: Satisfies SETT-02. Replaces the Company tab placeholder from 04-01.
Output: frontend/components/settings/CompanyTab.tsx, page.tsx updated to import and render it.
</objective>

<execution_context>
@C:/Users/Admin3k/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin3k/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-settings-panel/04-RESEARCH.md
@.planning/phases/04-settings-panel/04-01-SUMMARY.md

<interfaces>
<!-- Design tokens: bg #020617, surface #0F172A, border #1E293B, primary #6366F1 -->
<!-- shadcn/ui: Input, Select, SelectTrigger, SelectContent, SelectItem, Label, Card -->
<!-- Colour picker: native <input type="color"> — no library -->
<!-- Logo upload: native <input type="file" accept="image/*"> — no library -->
<!-- Image preview: URL.createObjectURL(file) stored in useState<string|null> -->
<!-- CRITICAL: hex color values MUST use style={{ backgroundColor: brandColor }} never bg-[${brandColor}] -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CompanyTab component</name>
  <files>frontend/components/settings/CompanyTab.tsx</files>
  <action>
Create `frontend/components/settings/CompanyTab.tsx`. Mark `'use client'`.

**State:**
```typescript
const [logoFile, setLogoFile] = useState<File | null>(null);
const [logoPreview, setLogoPreview] = useState<string | null>(null);
const [brandColor, setBrandColor] = useState('#6366F1');
const [companyName, setCompanyName] = useState('');
const [about, setAbout] = useState('');
const [industry, setIndustry] = useState('');
const [website, setWebsite] = useState('');
const [hq, setHq] = useState('');
const [companySize, setCompanySize] = useState('');
```

**Logo upload handler:**
```typescript
function handleLogoChange(e: React.ChangeEvent<HTMLInputElement>) {
  const file = e.target.files?.[0] ?? null;
  setLogoFile(file);
  if (logoPreview) URL.revokeObjectURL(logoPreview);
  setLogoPreview(file ? URL.createObjectURL(file) : null);
}
// useEffect cleanup: return () => { if (logoPreview) URL.revokeObjectURL(logoPreview); }
```

**Layout:** Two-column form on lg screens (`grid grid-cols-1 lg:grid-cols-3 gap-8`). Left column (lg:col-span-1): logo upload card. Right column (lg:col-span-2): all fields.

**Logo upload card:** A dashed-border rounded box (border-border border-dashed). If `logoPreview`, show `<img src={logoPreview} className="w-full h-40 object-contain rounded-lg" />`. Else show upload icon (Upload from lucide) + "Upload company logo" text. Below the box, a hidden `<input type="file" accept="image/*">` triggered by clicking the box. File size hint: "PNG or SVG, max 2MB".

**Brand colour picker row:** Label "Brand Colour" + `<input type="color" value={brandColor} onChange={e => setBrandColor(e.target.value)} className="w-10 h-10 rounded cursor-pointer border border-border bg-transparent p-0.5" />` + a `<div style={{ backgroundColor: brandColor }} className="w-10 h-10 rounded border border-border" />` swatch showing the selected colour.

**Fields (right column):**
- Company Name: Input controlled
- About: `<textarea>` (4 rows) controlled — use `className` matching Input style (border border-border rounded-md bg-transparent px-3 py-2 text-sm w-full)
- Industry: Select with options: Technology, Finance, Healthcare, Retail, Education, Media, Other
- Website: Input type="url"
- HQ Location: Input
- Company Size: Select with options: 1–10, 11–50, 51–200, 201–500, 500+

Wrap each field in a `<div className="flex flex-col gap-1.5">` with a `<label className="text-sm font-medium text-foreground">` above it.

Add a save button row at bottom: `<button className="px-4 py-2 bg-indigo-600 hover:bg-indigo-500 text-white text-sm font-medium rounded-lg transition-colors">Save Changes</button>` — onClick does nothing (UI shell, SETT-09).

CRITICAL: The brand colour swatch MUST use `style={{ backgroundColor: brandColor }}` — never `bg-[${brandColor}]`.
  </action>
  <verify>
    <automated>cd /c/Users/Admin3k/Documents/interview/frontend && yarn tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>CompanyTab.tsx compiles with zero TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wire CompanyTab into settings page</name>
  <files>frontend/app/settings/page.tsx</files>
  <action>
Read `frontend/app/settings/page.tsx`. Import `CompanyTab` from `@/components/settings/CompanyTab`. Replace the Company TabsContent placeholder div with `<CompanyTab />`. Run yarn build to confirm no errors.
  </action>
  <verify>
    <automated>cd /c/Users/Admin3k/Documents/interview/frontend && yarn build 2>&1 | tail -20</automated>
  </verify>
  <done>yarn build passes. Company tab renders CompanyTab component (not placeholder text).</done>
</task>

</tasks>

<verification>
- `frontend/components/settings/CompanyTab.tsx` exists
- No dynamic Tailwind class interpolation for hex colours anywhere in the file
- No Supabase imports or write calls
- yarn build passes
</verification>

<success_criteria>
Company tab shows logo upload area, brand colour picker with live swatch update, and all six profile fields as interactive inputs. No values persist on page refresh.
</success_criteria>

<output>
After completion, create `.planning/phases/04-settings-panel/04-02-SUMMARY.md`
</output>
