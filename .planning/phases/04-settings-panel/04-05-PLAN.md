---
phase: 04-settings-panel
plan: 05
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - frontend/components/settings/JobBoardsTab.tsx
  - frontend/app/settings/page.tsx
autonomous: false
requirements: [SETT-08, SETT-09]

must_haves:
  truths:
    - "Job Boards tab shows four platform cards: LinkedIn, Indeed, Glassdoor, Bayt.com"
    - "Each card shows platform name, a status label (Not Connected), and a Switch toggle"
    - "Toggling a Switch updates local state — no OAuth flow triggered"
    - "No Supabase writes anywhere in this tab"
  artifacts:
    - path: "frontend/components/settings/JobBoardsTab.tsx"
      provides: "Job Boards tab with four platform connection cards and Switch toggles"
  key_links:
    - from: "frontend/app/settings/page.tsx"
      to: "frontend/components/settings/JobBoardsTab.tsx"
      via: "import + TabsContent replacement"
---

<objective>
Implement the Job Boards tab: four platform connection cards (LinkedIn, Indeed, Glassdoor, Bayt.com) each showing connection status and a Switch toggle. No OAuth, no persistence. Then verify the complete /settings page end-to-end.

Purpose: Satisfies SETT-08, SETT-09. Completes Phase 4 Settings Panel.
Output: JobBoardsTab.tsx, page.tsx wired with all five tabs, human checkpoint to verify the full settings page.
</objective>

<execution_context>
@C:/Users/Admin3k/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin3k/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-settings-panel/04-RESEARCH.md
@.planning/phases/04-settings-panel/04-01-SUMMARY.md

<interfaces>
<!-- shadcn/ui Switch: import { Switch } from '@/components/ui/switch' -->
<!-- Design tokens: surface #0F172A, border #1E293B, emerald #10B981 (connected), muted for Not Connected -->
<!-- JOB_BOARDS constant:
const JOB_BOARDS = [
  { id: 'linkedin',  name: 'LinkedIn',  subtitle: 'Professional network', color: '#0077B5' },
  { id: 'indeed',    name: 'Indeed',    subtitle: 'Job search engine',    color: '#2164F3' },
  { id: 'glassdoor', name: 'Glassdoor', subtitle: 'Company reviews & jobs', color: '#0CAA41' },
  { id: 'bayt',      name: 'Bayt.com',  subtitle: 'MENA job platform',    color: '#E8453C' },
];
-->
<!-- CRITICAL: No OAuth redirect on toggle. No Supabase writes. Switch toggling is purely local state. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JobBoardsTab component</name>
  <files>frontend/components/settings/JobBoardsTab.tsx</files>
  <action>
Create `frontend/components/settings/JobBoardsTab.tsx`. Mark `'use client'`.

**State:**
```typescript
const [enabled, setEnabled] = useState<Record<string, boolean>>({
  linkedin: false,
  indeed: false,
  glassdoor: false,
  bayt: false,
});
```

**JOB_BOARDS constant (module-level):**
```typescript
const JOB_BOARDS = [
  { id: 'linkedin',  name: 'LinkedIn',  subtitle: 'Professional network',   color: '#0077B5' },
  { id: 'indeed',    name: 'Indeed',    subtitle: 'Job search engine',       color: '#2164F3' },
  { id: 'glassdoor', name: 'Glassdoor', subtitle: 'Company reviews & jobs',  color: '#0CAA41' },
  { id: 'bayt',      name: 'Bayt.com',  subtitle: 'MENA job platform',       color: '#E8453C' },
];
```

**Layout:**
- Section header: "Job Board Integrations"
- Subtitle text: "Connect your job boards to automatically sync listings. OAuth integration coming in v2.1."
- Grid: `<div className="grid grid-cols-1 sm:grid-cols-2 gap-4">` (2-column on sm+)

**Each card:**
```tsx
<div
  key={board.id}
  className="flex items-center justify-between p-5 rounded-xl border border-border bg-[#0F172A]"
>
  <div className="flex items-center gap-4">
    {/* Coloured initial/logo block */}
    <div
      className="w-10 h-10 rounded-lg flex items-center justify-center text-white text-sm font-bold shrink-0"
      style={{ backgroundColor: board.color }}
    >
      {board.name[0]}
    </div>
    <div>
      <div className="text-sm font-semibold text-foreground">{board.name}</div>
      <div className="text-xs text-muted-foreground">{board.subtitle}</div>
      {/* Status label */}
      <div className={`text-xs mt-0.5 ${enabled[board.id] ? 'text-emerald-400' : 'text-muted-foreground'}`}>
        {enabled[board.id] ? 'Enabled' : 'Not Connected'}
      </div>
    </div>
  </div>
  <Switch
    checked={enabled[board.id]}
    onCheckedChange={(val) => setEnabled(prev => ({ ...prev, [board.id]: val }))}
  />
</div>
```

Add a bottom note: `<p className="text-xs text-muted-foreground mt-4">Enabling a board will not automatically post jobs until OAuth is configured in v2.1.</p>`

CRITICAL: No OAuth redirect logic. No Supabase imports. No write calls.
  </action>
  <verify>
    <automated>cd /c/Users/Admin3k/Documents/interview/frontend && yarn tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>JobBoardsTab.tsx compiles with zero TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wire JobBoardsTab and run final build</name>
  <files>frontend/app/settings/page.tsx</files>
  <action>
Read `frontend/app/settings/page.tsx`. Import `JobBoardsTab` from `@/components/settings/JobBoardsTab`. Replace the Job Boards TabsContent placeholder with `<JobBoardsTab />`. Run yarn build to confirm full settings page compiles cleanly with all five tabs wired.
  </action>
  <verify>
    <automated>cd /c/Users/Admin3k/Documents/interview/frontend && yarn build 2>&1 | tail -20</automated>
  </verify>
  <done>yarn build passes. All five tabs are wired in settings page (no placeholder text remaining).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete /settings page with five tabs:
- Company: logo upload, colour picker, six profile fields
- Email & Comms: Gmail status, sender field, four template editors with placeholder pills, three send rule toggles
- Interviews: round count selector, per-round config rows (theme/duration/voice/avatar)
- Scoring: four preset cards (Growth/Standard/High Bar/Elite), per-round threshold inputs
- Job Boards: four platform cards (LinkedIn, Indeed, Glassdoor, Bayt.com) with Switch toggles
  </what-built>
  <how-to-verify>
1. Run `cd frontend && yarn dev` and navigate to http://localhost:3000/settings
2. Confirm sidebar shows a Settings link and clicking it navigates to /settings
3. Click each of the five tabs — confirm content changes without page reload
4. Company tab: Upload an image file — confirm preview appears. Change colour picker — confirm swatch updates.
5. Email & Comms tab: Click each of the four template buttons — confirm switching works and values are preserved when switching back. Toggle each send rule Switch.
6. Interviews tab: Click 1 / 2 / 3 round buttons — confirm rows appear/disappear. Change selects in a round row.
7. Scoring tab: Click each preset card — confirm border changes colour. Adjust a threshold number input.
8. Job Boards tab: Toggle each Switch — confirm status label changes between "Not Connected" and "Enabled".
9. Refresh the page — confirm all values have reset (UI shell only, SETT-09).
10. Sign out and navigate to /settings directly — confirm redirect to /login.
  </how-to-verify>
  <resume-signal>Type "approved" when all five tabs verified, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
- JobBoardsTab.tsx exists
- All five tab components imported in settings/page.tsx (no placeholder divs remaining)
- No Supabase write calls in any settings component
- yarn build passes
- Human verification: all five tabs interactive and values reset on refresh
</verification>

<success_criteria>
/settings renders all five tabs. Each tab is interactive. No values persist on page refresh. Unauthenticated users redirected to /login. No console errors during normal interaction.
</success_criteria>

<output>
After completion, create `.planning/phases/04-settings-panel/04-05-SUMMARY.md`
</output>
